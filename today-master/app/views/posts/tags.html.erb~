<div class="row">
  <div class="col-md-3">
    <%= render 'layouts/today_logo' %>
  </div>
  <div class="col-md-6">
    <h3>Showing Posts for tag <%= @tag.name %></h3>

    <% if @posts && @posts.any? %>
      <% for @posts.each do |post| %>
        <div class="panel panel-default">
          <div class="panel-heading">
            <%= post.user.username %> has:
            <%= post.title %>
          </div>
          <div class="panel-body">
            <%= post.body %>
          </div>
          <div class="panel-footer">
            Tags: <%= render_tags_as_links(post.taglist) %>
          </div>
        </div>
      <% end %>
    <% else %>
      <p>No posts.</p>
    <% end %>

  </div>
  <div class="col-md-3">
    <h3>All Tags</h3>

  </div>
</div>
